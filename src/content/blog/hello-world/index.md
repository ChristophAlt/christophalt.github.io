---
title: "Hello world"
description: "A placeholder draft so the blog collection is never empty."
date: 2026-09-16
draft: true
---

This post is a draft and is not published. It exists so the `blog` collection is
never empty, which would otherwise break the build. Copy it to start a real post,
and delete it once you have one.
